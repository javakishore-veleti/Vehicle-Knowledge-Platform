fastapi>=0.111,<0.116
starlette>=0.37,<0.42   # pin: crewai's mcp/chromadb pull starlette 1.x, which breaks fastapi 0.115
uvicorn[standard]>=0.30,<0.35
fastembed>=0.3,<0.6
psycopg2-binary>=2.9
pydantic>=2.6
pymongo>=4.6
openai>=1.30
langgraph>=0.2,<0.6
boto3>=1.34
cryptography>=42.0
prometheus-client>=0.20
# OpenTelemetry distributed tracing (FastAPI + urllib auto-instrumentation, OTLP/HTTP -> Jaeger)
opentelemetry-sdk>=1.27
opentelemetry-exporter-otlp-proto-http>=1.27
opentelemetry-instrumentation-fastapi>=0.48b0
opentelemetry-instrumentation-urllib>=0.48b0
crewai>=1.0          # the 'crewai' framework (2-agent crew); heavy
litellm>=1.80        # crewai's LLM fallback for non-native providers (Groq)
llama-index-core>=0.11,<0.13   # the 'llamaindex' framework (RetrieverQueryEngine)
llama-index-llms-litellm       # LlamaIndex -> Groq via litellm
haystack-ai>=2.0     # the 'haystack' framework (RAG Pipeline)

