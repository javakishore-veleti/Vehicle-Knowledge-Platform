apiVersion: apps/v1
kind: Deployment
metadata:
  name: company-service
  labels: { app: company-service, tier: java }
spec:
  replicas: 1
  selector:
    matchLabels: { app: company-service }
  template:
    metadata:
      labels: { app: company-service, tier: java }
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: company-service
          image: vkp/company-service:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: vkp-config
            - secretRef:
                name: vkp-secrets
          readinessProbe:
            httpGet: { path: /actuator/health, port: 8081 }
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health, port: 8081 }
            initialDelaySeconds: 40
            periodSeconds: 20
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits: { memory: 1Gi }
---
apiVersion: v1
kind: Service
metadata:
  name: company-service
  labels: { app: company-service }
spec:
  selector: { app: company-service }
  ports:
    - port: 8081
      targetPort: 8081
