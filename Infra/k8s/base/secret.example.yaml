# TEMPLATE — copy to secret.yaml, fill real values, and DO NOT commit secret.yaml (gitignored).
# In production use a real secrets manager (Sealed Secrets / External Secrets / SOPS / cloud KMS),
# not a plain Secret. JWT_SECRET + VKP_SESSION_SECRET MUST be identical across all services.
apiVersion: v1
kind: Secret
metadata:
  name: vkp-secrets
type: Opaque
stringData:
  JWT_SECRET: "change-me-dev-secret-please-override-in-prod-0123456789"
  VKP_SESSION_SECRET: ""        # base64 of a 32-byte AES key (blank => ephemeral; set in prod)
  SPRING_DATASOURCE_PASSWORD: "vkp"
  VKP_PG_PASSWORD: "vkp"
  WFS_PG_PASSWORD: "vkp"
  OPENAI_API_KEY: ""
  GROQ_API_KEY: ""
