AWSTemplateFormatVersion: "2010-09-09"
Description: >
  VKP AWS_003 ECR. One ECR repository per VKP service/portal image (16 total). Scan-on-push,
  keep-last-10 lifecycle (caps storage cost), and EmptyOnDelete so the Destroy workflow can
  delete the stack even when images are present.

Resources:
  EcrVkpCompanyService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-company-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpUserService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-user-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpAirflowAdapterService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-airflow-adapter-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpDataCollectionService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-data-collection-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpIngestionService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-ingestion-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpIndexingService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-indexing-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpIndexingWfs:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-indexing-wfs
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpVectorConfigService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-vector-config-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpVehicleExploreService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-vehicle-explore-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpGuardrailsService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-guardrails-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpAgenticService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-agentic-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpContextEngineService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-context-engine-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpContextAdminService:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-context-admin-service
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpAdminPortal:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-admin-portal
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpVehicleSearchPortal:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-vehicle-search-portal
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }
  EcrVkpCefPortal:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: vkp-cef-portal
      EmptyOnDelete: true
      ImageScanningConfiguration:
        ScanOnPush: true
      LifecyclePolicy:
        LifecyclePolicyText: |
          { "rules": [ { "rulePriority": 1, "description": "keep last 10",
            "selection": { "tagStatus": "any", "countType": "imageCountMoreThan", "countNumber": 10 },
            "action": { "type": "expire" } } ] }

Outputs:
  RegistryUri:
    Description: "ECR registry host for these repos (<account>.dkr.ecr.<region>.amazonaws.com)."
    Value: !Sub "${AWS::AccountId}.dkr.ecr.${AWS::Region}.amazonaws.com"
