# Context Engineering Framework — orchestrator service. Reuses VKP's pgvector retrieval (fastembed
# minilm) and talks to OpenAI/Groq for reasoning; MongoDB for memory (optional).
fastapi>=0.116
uvicorn[standard]>=0.30
pydantic>=2.7
psycopg2-binary>=2.9
fastembed>=0.4,<0.8
openai>=1.40
pymongo>=4.6
